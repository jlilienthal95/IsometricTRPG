[gd_scene load_steps=2 format=3]

[ext_resource type="PackedScene" uid="uid://whn3ehlnh1fk" path="res://Scenes/Battle/Units/Unit.tscn" id="1_base"]

; Knight's SpriteFrames happen to already be the ones baked into Unit.tscn's
; UnitSprite as its placeholder default (Unit.tscn's defaults were originally
; authored FROM Knight's art) — so sprite_frames itself needs no override
; here. The position offset below reproduces what the old runtime
; `unit_sprite.position += job.sprite_offset` step used to apply for every
; job, Knight included, so on-screen placement is unchanged from before.

[node name="Knight" instance=ExtResource("1_base")]

[node name="UnitSprite" parent="VisualRoot" index="0"]
position = Vector2(-30.000002, -56.000004)
